# Estado del proyecto — ÚNICA fuente de verdad.
# Ningún agente responde "cómo vamos" de memoria: lo lee de aquí.
#
# Todo lo que describe este archivo es entorno de DESARROLLO. No hay CI, no hay
# despliegue y no hay release: de subir el proyecto se encarga el usuario.

proyecto:
  nombre:
  fase: discovery          # discovery | plan | build | audit | qa
  tipo:                    # SPA | ERP | CRM | panel | landing | API pública | ...
                           # Lo cierra el arquitecto en el discovery, en palabras del dominio.
  actualizado:

# Instancias del repositorio. paths.sh las detecta solas leyendo el disco;
# esto es el registro legible de qué es cada una y quién habla con quién.
instancias:
  backends: []
  # - nombre: ventas_backend
  #   contrato: docs/contracts/ventas_backend.md
  #   consumido_por: [cliente_frontend]
  frontends: []
  # - nombre: cliente_frontend
  #   consume: [ventas_backend]
  #   puerto_dev: 5173

historias: []
# - id: STORY-001
#   titulo: Listado de productos
#   estado: en_curso
#   tareas: [TASK-012, TASK-014]

tareas: []
# - id: TASK-014
#   titulo: Endpoint GET /api/products con filtros
#   instancia: ventas_backend
#   agente: backend-developer
#   estado: en_curso        # pendiente | en_curso | en_auditoria | bloqueada | hecha
#   depende_de: [TASK-012]
#   auditoria: docs/audits/AUDIT-003.md
#   veredicto: aprobado

bloqueos: []
# - tarea: TASK-018
#   motivo: "Falta decidir si el borrado es lógico o físico"
#   necesita: decisión humana

deuda: []
# - descripcion: "ProductService supera 200 líneas, mezcla precios e inventario"
#   origen: TASK-014
#   prioridad: media

decisiones_pendientes: []
# - pregunta: "¿El borrado de un pedido es lógico o físico?"
#   bloquea: [TASK-018]
#   nota: "Afecta al modelo de datos; decidir antes de la migración."
